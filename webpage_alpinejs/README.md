# Commands
Deploy the server in DEV mode:
```
deno task dev
```

Compile the CSS using TailwindCSS (download the executable from https://github.com/tailwindlabs/tailwindcss/releases):
```
static/css/tailwindcss-linux-x64 -i static/css/input.css -o static/css/output.css
```
